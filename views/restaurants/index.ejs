<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>
<!-- iterate over tacos and for each print name with options of tasty/not tasty -->
<section>
    <h1>Restaurants! 🌮🌮🌮</h1>
    <% if (user) { %>
      <h2>Add a review!</h2>
      <form action="/resturants" method="POST">
        <label>
          Name: 
          <input type="text" name="name" autocomplete="off">
        </label>
        <label>
          Would Recommend?
          <input type="checkbox" name="yes">
        </label>
        <button type="submit">Add Resturant🌮</button>
      </form>
  <% } %>

  <h2>All Restaurants</h2>
  <% restaurants.forEach(restaurant => { %>
    <li>
      🌮 
      <a href="/resturants/<%= resturant._id %>"><%= resturant.name %></a> 
      is <%= resturant.wouldRecommend ? 'Yes!' : 'No Way!' %>
    </li>
  <% }) %>
<%- include('../partials/footer') %>